<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./vault/server`

Source: `src/vault/server.ts`

8 exports.

### `assessVaultDeletionBatch`

`function` — Decide whether a reconciliation pass's proposed deletions are safe to apply against the live baseline.

```ts
(input: { baselinePaths: readonly string[]; proposedDeletions: readonly string[]; manifestEmpty?: boolean | undefined;…
```

### `compareIncarnationBaseline`

`function` — Compare a recorded baseline filesystem-incarnation id against a sandbox's current incarnation fields.

```ts
(baselineId: string | undefined, current: FilesystemIncarnationLike) => IncarnationComparison
```

### `FilesystemIncarnationLike`

`interface` — The subset of a sandbox's filesystem-incarnation identity this module needs.

```ts
interface FilesystemIncarnationLike
```

### `IncarnationComparison`

`type` — Result of comparing a recorded baseline incarnation id against the sandbox's current identity.

```ts
type IncarnationComparison
```

### `VAULT_DELETION_REFUSAL_MIN_LIVE_FILES`

`const` — The blast-radius ratio gate only engages once the live baseline is at least this large, so a 2-file vault losing 1 file (50%) is not refused on ratio alone.

```ts
10
```

### `VAULT_DELETION_REFUSAL_RATIO`

`const` — Refuse a deletion batch once it would remove at least this fraction of the live baseline (both this and `VAULT_DELETION_REFUSAL_MIN_LIVE_FILES` must hold — a tiny vault can lose all its files without…

```ts
0.75
```

### `VaultDeletionAssessment`

`interface`

```ts
interface VaultDeletionAssessment
```

### `VaultDeletionPolicy`

`interface` — Overrides for `assessVaultDeletionBatch`'s two thresholds.

```ts
interface VaultDeletionPolicy
```
