<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./openui-react`

Source: `src/openui-react/index.tsx`

8 exports.

### `OPENUI_RESERVED_BODY_KEYS`

`const` — The body keys the hook owns; a product's extra `body` fields cannot shadow them.

```ts
readonly string[]
```

### `OpenUIActionContext`

`interface` — Where on the page an action fired, and what the form held at that moment.

```ts
interface OpenUIActionContext
```

### `OpenUIActionFailure`

`interface` — A refused action, as the route sends it.

```ts
interface OpenUIActionFailure
```

### `OpenUIActionOutcome`

`type` — The boundary outcome: the reply, or the reason there is none.

```ts
type OpenUIActionOutcome
```

### `OpenUIActionResponse`

`interface` — A successful action reply, as the route sends it.

```ts
interface OpenUIActionResponse
```

### `OpenUIActionsController`

`interface` — What the hook returns.

```ts
interface OpenUIActionsController
```

### `useOpenUIActions`

`function` — Make an agent-authored page interactive.

```ts
<TNode extends OpenUINode = OpenUINode>(options: UseOpenUIActionsOptions<TNode>) => OpenUIActionsController<TNode>
```

### `UseOpenUIActionsOptions`

`interface` — How the hook talks to the product route.

```ts
interface UseOpenUIActionsOptions
```
