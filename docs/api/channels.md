<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./channels`

Source: `src/channels/index.ts`

46 exports.

### `ChannelConnect`

`function` — Select an existing line or connect an owned source, then verify it.

```ts
({ transport, initialLineId, className }: ChannelConnectProps) => Element
```

### `ChannelConnection`

`interface`

```ts
interface ChannelConnection
```

### `ChannelConnectProps`

`interface`

```ts
interface ChannelConnectProps
```

### `ChannelConversation`

`function` — Read-only history.

```ts
({ lineId, threadId, className }: { lineId: string; threadId: string; className?: string | undefined; }) => Element
```

### `ChannelConversations`

`function` — A channel's separate member threads.

```ts
({ lineId, className }: { lineId: string; className?: string | undefined; }) => Element
```

### `channelMessageLink`

`function` — User-facing deep links, not provider API calls.

```ts
(line: Line, instruction: string) => string | null
```

### `ChannelNumber`

`interface`

```ts
interface ChannelNumber
```

### `ChannelReadContext`

`interface`

```ts
interface ChannelReadContext
```

### `ChannelsClient`

`interface` — The one authenticated, agent-scoped boundary.

```ts
interface ChannelsClient
```

### `ChannelsProvider`

`function` — Mount below authentication.

```ts
({ client, pollInterval, children }: { client: ChannelsClient; pollInterval?: number | false | undefined; children: Rea…
```

### `ChannelVerification`

`interface` — Public evidence only.

```ts
interface ChannelVerification
```

### `ChannelVerificationPanel`

`function` — Delivery evidence comes exclusively from the host.

```ts
({ lineId, className }: { lineId: string; className?: string | undefined; }) => Element
```

### `ConnectChannelInput`

`type`

```ts
type ConnectChannelInput
```

### `EmailChannel`

`function`

```ts
(props: TransportPanelProps) => Element
```

### `holdsNumber`

`function`

```ts
(order: HubNumberOrder) => boolean
```

### `HubNumberOrder`

`interface`

```ts
interface HubNumberOrder
```

### `HubNumberQuote`

`interface`

```ts
interface HubNumberQuote
```

### `HubNumberReadiness`

`interface`

```ts
interface HubNumberReadiness
```

### `IMessageChannel`

`function`

```ts
(props: TransportPanelProps) => Element
```

### `Line`

`interface`

```ts
interface Line
```

### `LineAttachment`

`interface`

```ts
interface LineAttachment
```

### `LineFromConnectionInput`

`type` — Input to `client.lines.fromConnection()`.

```ts
type LineFromConnectionInput
```

### `LineMessage`

`interface`

```ts
interface LineMessage
```

### `LinePayment`

`interface`

```ts
interface LinePayment
```

### `LinePayPage`

`function` — End-user allowance/checkout page, not number acquisition or creator payouts.

```ts
({ lineId, className }: { lineId: string; className?: string | undefined; }) => Element
```

### `LineThread`

`interface`

```ts
interface LineThread
```

### `LineTransport`

`type` — The transport a line carries.

```ts
type LineTransport
```

### `NUMBER_CHARGE_NOTICE`

`const`

```ts
"This is a one-time activation charge. Tangle does not bill recurring carrier rental for this number. Your provider’s m…
```

### `NUMBER_RETRY_NOTICE`

`const`

```ts
"Retry uses the same approved quote, not a new purchase. Do not order again while the result is unknown."
```

### `NumberChannel`

`function` — A transport's managed number activation, with explicit consent and cancellation.

```ts
({ transport, className }: { transport: "sms" | "imessage"; className?: string | undefined; }) => Element
```

### `numberStage`

`function`

```ts
(order: HubNumberOrder) => string
```

### `safeCheckoutUrl`

`function` — Only same-origin relative paths or credential-free HTTPS checkout URLs.

```ts
(value: string) => string
```

### `SMSChannel`

`function`

```ts
(props: TransportPanelProps) => Element
```

### `useChannel`

`function`

```ts
(lineId: string) => { run: (input: { action: VerificationAction; confirm?: boolean | undefined; }) => Promise<MutationO…
```

### `useChannelConnections`

`function`

```ts
(transport: LineTransport) => AsyncResourceState<ChannelConnection[]>
```

### `useChannelConversation`

`function`

```ts
(lineId: string, threadId: string) => AsyncResourceState<LineMessage[]>
```

### `useChannelConversations`

`function`

```ts
(lineId: string) => AsyncResourceState<LineThread[]>
```

### `useChannels`

`function`

```ts
() => AsyncResourceState<Line[]>
```

### `useChannelsClient`

`function`

```ts
() => ChannelsClient
```

### `useConnectChannel`

`function` — Connect only acquires/binds a line.

```ts
(onConnected?: ((line: Line) => void) | undefined) => { run: (input: ConnectChannelInput) => Promise<MutationOutcome<Li…
```

### `useLinePayment`

`function`

```ts
(lineId: string) => { available: boolean; run: (input: { consent: boolean; }) => Promise<MutationOutcome<string>>; stat…
```

### `useNumberChannel`

`function` — Hub owns prices, terms, idempotency, funding, and cancellation.

```ts
(transport: "sms" | "imessage") => { run: (input: { action: "quote"; } | { action: "purchase"; consent: boolean; } | {…
```

### `useWhatsAppNumbers`

`function`

```ts
(connectionId: string) => AsyncResourceState<ChannelNumber[]>
```

### `VerificationAction`

`type`

```ts
type VerificationAction
```

### `verificationExpired`

`function`

```ts
(test: ChannelVerification, now?: number) => boolean
```

### `WhatsAppChannel`

`function`

```ts
(props: TransportPanelProps) => Element
```
