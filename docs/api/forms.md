<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./forms`

Source: `src/forms/index.ts`

29 exports.

### `assertFormBlankIntegrity`

`function` — Prove the embedded bytes are the file the registry was derived from.

```ts
(blank: FormBlank) => Promise<void>
```

### `checkRegistryAgainstBlank`

`function` — Check a registry against the blank it claims to describe.

```ts
(args: { pdf: Uint8Array<ArrayBufferLike>; registry: FormRegistry; }) => Promise<RegistryCheckResult>
```

### `decodeFormBlank`

`function` — Decode a blank once per isolate.

```ts
(blank: FormBlank) => Uint8Array<ArrayBufferLike>
```

### `describeFormFill`

`function` — What to tell the agent after a fill.

```ts
(result: FillFormResult) => string
```

### `FilledWidget`

`interface` — One widget the fill actually wrote.

```ts
interface FilledWidget
```

### `FillFormOptions`

`interface`

```ts
interface FillFormOptions
```

### `FillFormResult`

`interface`

```ts
interface FillFormResult
```

### `fillPdfForm`

`function` — Write a value map onto a blank, returning the bytes plus the exact widget-by-widget account of what was and was not placed.

```ts
(options: FillFormOptions) => Promise<FillFormResult>
```

### `formatFormCurrency`

`function` — Format a figure the way a US agency form prints it: grouped thousands, two decimals, negatives in parentheses.

```ts
(value: number) => string
```

### `FormBlank`

`interface` — A blank form's bytes, base64-encoded, with the provenance to check them.

```ts
interface FormBlank
```

### `FormLabelBasis`

`type` — Where a slot's `label` came from, and therefore what it can prove.

```ts
type FormLabelBasis
```

### `FormRegistry`

`interface` — A form's complete slot table, pinned to the revision it was derived from.

```ts
interface FormRegistry
```

### `FormSlot`

`interface` — One semantic slot: what a caller supplies, and where the platform puts it.

```ts
interface FormSlot
```

### `FormSlotFormat`

`type` — How a slot's text value is rendered into the box.

```ts
type FormSlotFormat
```

### `FormSlotKind`

`type` — What kind of widget a slot writes to.

```ts
type FormSlotKind
```

### `LabelVerdict`

`type` — What the slot's `label` is worth, checked against the produced file.

```ts
type LabelVerdict
```

### `parseFormAmount`

`function` — A number, or a number written the way a form prints one (`$141,318.74`).

```ts
(value: unknown) => number | undefined
```

### `parseFormBoolean`

`function` — A checkbox takes a boolean and nothing else.

```ts
(value: unknown) => boolean | undefined
```

### `RegistryCheckResult`

`interface`

```ts
interface RegistryCheckResult
```

### `RegistryProblem`

`interface`

```ts
interface RegistryProblem
```

### `RegistryProblemCode`

`type` — Why a registry does not match the blank it claims to describe.

```ts
type RegistryProblemCode
```

### `sha256Hex`

`function` — Lowercase-hex SHA-256 of a byte range.

```ts
(bytes: Uint8Array<ArrayBufferLike>) => Promise<string>
```

### `SlotVerdict`

`type`

```ts
type SlotVerdict
```

### `SlotVerification`

`interface`

```ts
interface SlotVerification
```

### `UnfilledCode`

`type` — Why a supplied value did not reach the page.

```ts
type UnfilledCode
```

### `UnfilledSlot`

`interface`

```ts
interface UnfilledSlot
```

### `verifyFilledForm`

`function` — Verify a filled form against the value map it was filled from.

```ts
(args: { pdf: Uint8Array<ArrayBufferLike>; registry: FormRegistry; expected: Record<string, unknown>; }) => Promise<Ver…
```

### `VerifyFormResult`

`interface`

```ts
interface VerifyFormResult
```

### `widgetLabel`

`function` — The `/TU` accessibility text a widget carries, if any.

```ts
(form: PDFForm, field: string) => Promise<string | undefined>
```
