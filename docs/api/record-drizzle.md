<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./record/drizzle`

Source: `src/record/drizzle.ts`

22 exports.

### `createRecordStore`

`function` — Build a store bound to one table and one policy.

```ts
(options: CreateRecordStoreOptions) => RecordStore
```

### `CreateRecordStoreOptions`

`interface` — Options for {@link createRecordStore}.

```ts
interface CreateRecordStoreOptions
```

### `createRecordTable`

`function` — Build one record-entry table wired to the product's tables.

```ts
{ <TExtra extends RecordExtraColumns>(options: CreateRecordTableOptions & { extraColumns: TExtra; }): RecordTableWithEx…
```

### `CreateRecordTableOptions`

`interface` — Options for {@link createRecordTable}.

```ts
interface CreateRecordTableOptions
```

### `ListRecordEntriesInput`

`interface` — Filters for {@link RecordStore.list}.

```ts
interface ListRecordEntriesInput
```

### `MaterializeRecordInput`

`interface` — Input to {@link RecordStore.materialize}.

```ts
interface MaterializeRecordInput
```

### `MaterializeRecordResult`

`interface` — What a materialization produced.

```ts
interface MaterializeRecordResult
```

### `ReadRecordHeadsInput`

`interface` — Input to {@link RecordStore.heads}.

```ts
interface ReadRecordHeadsInput
```

### `RECORD_STORE_COLUMNS`

`const` — Column property names the store owns.

```ts
ReadonlySet<string>
```

### `RecordAtomicStrategy`

`type` — How this store executes a two-statement atomic unit on the caller's driver.

```ts
type RecordAtomicStrategy
```

### `RecordDatabase`

`type` — Any SQLite drizzle database.

```ts
type RecordDatabase
```

### `RecordEntryRow`

`type` — One stored entry, as selected.

```ts
type RecordEntryRow
```

### `RecordExtraColumns`

`type` — Product columns added to the entry table, keyed by the property name the store and the product read them under.

```ts
type RecordExtraColumns
```

### `RecordParentTable`

`type` — A product table referenced by foreign key — only its `id` column is used.

```ts
type RecordParentTable
```

### `RecordStore`

`interface` — The store surface.

```ts
interface RecordStore
```

### `RecordTable`

`type` — The table shape {@link createRecordTable} produces.

```ts
type RecordTable
```

### `RecordTableWithExtras`

`type` — The record table plus the product's own columns, so a product's direct queries name them and the store still sees the shape it depends on.

```ts
type RecordTableWithExtras
```

### `resolveAtomicStrategy`

`function` — Pick the driver's atomic primitive.

```ts
(db: RecordDatabase) => RecordAtomicStrategy
```

### `ReviewRecordEntryInput`

`interface` — Input to {@link RecordStore.review}.

```ts
interface ReviewRecordEntryInput
```

### `toFoldable`

`function` — Project a stored row onto the slice the pure fold reads.

```ts
(row: { path: string; id: string; createdAt: number; seq: number; dimension: string; period: number; conflict: boolean;…
```

### `WriteRecordEntryInput`

`interface` — Input to {@link RecordStore.write}.

```ts
interface WriteRecordEntryInput
```

### `WriteRecordEntryResult`

`interface` — What a write produced.

```ts
interface WriteRecordEntryResult
```
