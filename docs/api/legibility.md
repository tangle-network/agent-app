<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./legibility`

Source: `src/legibility/index.ts`

33 exports.

### `BannedTerm`

`interface` — A banned term plus the sentence that replaces it.

```ts
interface BannedTerm
```

### `buildScannedFile`

`function` — Lex an in-memory source — the shape every check test uses.

```ts
(path: string, text: string) => ScannedFile
```

### `checkLegibility`

`function` — Run the gate over a product's source.

```ts
(config: LegibilityConfig) => LegibilityReport
```

### `COPY_CALLS`

`const` — Calls whose first string argument reaches the reader.

```ts
readonly string[]
```

### `COPY_KEYS`

`const` — Attributes and object keys whose string value is copy.

```ts
readonly string[]
```

### `DEFAULT_BANNED_TERMS`

`const` — The default list.

```ts
readonly BannedTerm[]
```

### `EmptyStateOptions`

`interface`

```ts
interface EmptyStateOptions
```

### `formatLegibilityReport`

`function`

```ts
(report: LegibilityReport, options?: FormatOptions) => string
```

### `FormatOptions`

`interface`

```ts
interface FormatOptions
```

### `JsxAttribute`

`interface` — One attribute on a JSX element.

```ts
interface JsxAttribute
```

### `JsxElement`

`interface` — One JSX element, with the span every subtree question needs.

```ts
interface JsxElement
```

### `LEGIBILITY_CHECKS`

`const`

```ts
readonly LegibilityCheckId[]
```

### `LegibilityCheckId`

`type` — The checks this gate runs.

```ts
type LegibilityCheckId
```

### `LegibilityConfig`

`interface` — What a product writes in `legibility.config.mjs`.

```ts
interface LegibilityConfig
```

### `LegibilityFinding`

`interface` — One defect, at one place, with the fix named.

```ts
interface LegibilityFinding
```

### `LegibilityReport`

`interface`

```ts
interface LegibilityReport
```

### `legibilityReportToJson`

`function` — The machine-readable form, for a product that posts findings somewhere.

```ts
(report: LegibilityReport) => string
```

### `LegibilitySuppression`

`interface` — A suppression that was honoured, with the reason its author wrote.

```ts
interface LegibilitySuppression
```

### `parseRouteConfig`

`function` — Parse a `@react-router/dev/routes` config into composed route paths.

```ts
(file: ScannedFile) => RouteEntry[]
```

### `ReachabilityOptions`

`interface`

```ts
interface ReachabilityOptions
```

### `READERLESS_PATHS`

`const` — Paths whose modules cannot render.

```ts
readonly string[]
```

### `RouteEntry`

`interface` — One route as declared, with its composed path.

```ts
interface RouteEntry
```

### `scanFile`

`function` — Read and lex one file.

```ts
(path: string) => ScannedFile | null
```

### `ScannedFile`

`interface` — One file, lexed once, with its line index ready.

```ts
interface ScannedFile
```

### `ScannedSource`

`interface` — Everything one pass over a file produces.

```ts
interface ScannedSource
```

### `scanSource`

`function` — Lex one TS/TSX source.

```ts
(text: string) => ScannedSource
```

### `scanSources`

`function` — Lex every scannable file under `srcDirs`.

```ts
(srcDirs: readonly string[], ignore?: readonly string[]) => ScannedFile[]
```

### `SegmentKind`

`type` — What a stretch of source is, once the lexer has decided.

```ts
type SegmentKind
```

### `SilentFailureOptions`

`interface`

```ts
interface SilentFailureOptions
```

### `SourceSegment`

`interface` — One classified stretch of source.

```ts
interface SourceSegment
```

### `staticSegments`

`function` — The comparable segments of a path: statics only, params and holes dropped.

```ts
(path: string) => string[]
```

### `SuccessOptions`

`interface`

```ts
interface SuccessOptions
```

### `VocabularyOptions`

`interface`

```ts
interface VocabularyOptions
```
