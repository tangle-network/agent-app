<!-- GENERATED by scripts/gen-codemap.mjs — DO NOT EDIT BY HAND. Run `pnpm docs:gen`. -->

# `./run`

Source: `src/run/index.ts`

10 exports.

### `ExecutionMode`

`type`

```ts
type ExecutionMode
```

### `executionModeForProfile`

`function` — Execution mode for a profile — `harness` omitted/null => router.

```ts
(profile: ShellProfile) => ExecutionMode
```

### `isKnownSandboxHarness`

`function` — True when `harness` names a sandbox backend the SDK knows about.

```ts
(harness: ProfileHarness) => harness is "opencode" | "claude-code" | "nanoclaw" | "kimi-code" | "codex" | "amp" | "fact…
```

### `ProfileHarness`

`type`

```ts
type ProfileHarness
```

### `resolveExecutionMode`

`function` — Infer the execution mode from a profile's harness.

```ts
(harness: ProfileHarness) => ExecutionMode
```

### `ROUTER_HARNESS`

`const` — The router pseudo-harness — the absence of a sandbox backend.

```ts
"router"
```

### `RouterHarness`

`type`

```ts
type RouterHarness
```

### `runAgent`

`function` — Route a turn to the branch the harness selects and stream its events through.

```ts
<T>(harness: ProfileHarness, branches: RunAgentBranches<T>) => AsyncGenerator<T, any, any>
```

### `RunAgentBranches`

`interface` — The two branch runners.

```ts
interface RunAgentBranches
```

### `ShellProfile`

`interface` — The superset the dispatch reads: the SDK `AgentProfile` plus the harness discriminator.

```ts
interface ShellProfile
```
