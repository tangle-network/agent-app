<!-- GENERATED by scripts/gen-codemap.mjs — DO NOT EDIT BY HAND. Run `pnpm docs:gen`. -->

# `./teams/members-api`

Source: `src/teams/members-api.ts`

9 exports.

### `createMembersApi`

`function` — Build the members API bound to one product's db/tables/access.

```ts
(opts: MembersApiOptions) => { listMembers: (input: { workspaceId: string; actor: MembersApiActor; }) => Promise<Respon…
```

### `EnforceSeatSeam`

`interface` — Optional billing seat gate.

```ts
interface EnforceSeatSeam
```

### `MemberListEntry`

`interface` — Define the structure of a workspace member entry with identification, role, and status details

```ts
interface MemberListEntry
```

### `MembersApiActor`

`interface` — The authenticated caller — apps resolve this from their own session layer.

```ts
interface MembersApiActor
```

### `MembersApiOptions`

`interface` — Define configuration options for managing team members and workspace access APIs

```ts
interface MembersApiOptions
```

### `MemberSyncSeam`

`interface` — Optional membership-change propagation to an external system (e.g.

```ts
interface MemberSyncSeam
```

### `SeatLimitError`

`class` — Thrown by an `enforceSeat` seam to deny an invite; serialized to a 402.

```ts
class SeatLimitError
```

### `UserLookupTable`

`interface` — The product's user table, narrowed to the columns the member queries read.

```ts
interface UserLookupTable
```

### `WorkspaceLookupTable`

`interface` — The product's workspace table, narrowed to the columns the member queries read.

```ts
interface WorkspaceLookupTable
```
