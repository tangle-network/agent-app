<!-- GENERATED by agent-docs — DO NOT EDIT BY HAND. Regenerate with `agent-docs`. -->

# `./durable-chat/drizzle`

Source: `src/durable-chat/drizzle.ts`

20 exports.

### `createDrizzleDurableChatStore`

`function` — Build the composed store: both ports over all seven tables.

```ts
(options: CreateDrizzleDurableChatStoreOptions) => DurableChatStore
```

### `CreateDrizzleDurableChatStoreOptions`

`interface` — Options for the composed store.

```ts
interface CreateDrizzleDurableChatStoreOptions
```

### `createDrizzleInteractionStore`

`function` — Build the interaction-side store: ask projections and the answer-intent journal.

```ts
(options: CreateDrizzleInteractionStoreOptions) => DurableInteractionStore
```

### `CreateDrizzleInteractionStoreOptions`

`interface` — Options for the interaction-only store.

```ts
interface CreateDrizzleInteractionStoreOptions
```

### `createDrizzlePlanStore`

`function` — Build the plan-side store: projections, the command journal, the effect journal.

```ts
(options: CreateDrizzlePlanStoreOptions) => DurablePlanStore
```

### `CreateDrizzlePlanStoreOptions`

`interface` — Options for the plan-only store.

```ts
interface CreateDrizzlePlanStoreOptions
```

### `createDurableChatTables`

`function` — All seven tables, for a product wiring both halves.

```ts
() => DurableChatTables
```

### `createDurableInteractionTables`

`function` — The four tables an interaction-only adopter needs.

```ts
() => DurableInteractionTables
```

### `createDurablePlanTables`

`function` — The three tables a plan-only adopter needs.

```ts
() => DurablePlanTables
```

### `DURABLE_CHAT_MIGRATION_SQL`

`const` — The DDL these tables compile to, for products applying migrations by hand (D1, libsql).

```ts
"\nCREATE TABLE IF NOT EXISTS durable_plan_projection (\n id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),\n s…
```

### `DurableAnswerIntentRow`

`type` — A persisted answer intent.

```ts
type DurableAnswerIntentRow
```

### `DurableChatDatabase`

`type` — Any SQLite drizzle database — `any` erases driver-specific generics so better-sqlite3, D1 and libsql handles all fit.

```ts
type DurableChatDatabase
```

### `DurableChatStoreOptions`

`interface` — Shared construction options.

```ts
interface DurableChatStoreOptions
```

### `DurableChatTables`

`interface` — Both halves.

```ts
interface DurableChatTables
```

### `DurableInteractionProjectionRow`

`type` — A persisted interaction ask projection.

```ts
type DurableInteractionProjectionRow
```

### `DurableInteractionTables`

`interface` — Interaction-side tables.

```ts
interface DurableInteractionTables
```

### `DurablePlanCommandRow`

`type` — A persisted plan decision command.

```ts
type DurablePlanCommandRow
```

### `DurablePlanEffectRow`

`type` — A persisted after-decision effect claim.

```ts
type DurablePlanEffectRow
```

### `DurablePlanProjectionRow`

`type` — Row types, for products that query these tables directly.

```ts
type DurablePlanProjectionRow
```

### `DurablePlanTables`

`interface` — Plan-side tables.

```ts
interface DurablePlanTables
```
