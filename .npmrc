# Optional peers (@huggingface/transformers, drizzle-orm, react) must NOT be
# auto-installed: the transcription peer-missing test depends on
# @huggingface/transformers being absent, and every peer needed at dev time is
# already an explicit devDependency.
auto-install-peers=false
